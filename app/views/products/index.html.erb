<h1>Listing Products</h1> 
<p>
   <%# <%= link_to 'Search', search_posts_path %> Product
</p>
<table>
  <tr>
    <th>Name</th>
    <th>Description</th>
    <th>Price</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>
<% @products.each do |product| %>
  <tr>
    <td><%= product["NAME"] %></td>
    <td><%= product["DESCRIPTION"] %></td>
    <td><%= product["PRICE"] %></td>
    <td><%= link_to 'Show', product_path(product["ID"]) %></td>
  </tr>
<% end %>
</table>
<br />
<%= link_to 'New product', new_product_path %>

